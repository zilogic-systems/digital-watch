name: date
on entry: |
  view.update_display_date(model.time["current"])
  view.update_indication_state(model.enabled)
transitions:
  - target: time
    event: d_pressed

  - target: time
    guard: after(10)
